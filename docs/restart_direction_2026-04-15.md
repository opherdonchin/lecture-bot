# Restart Direction 2026-04-15

- Restart development from `main`.
- Preserve `diagnostics` as an archival and research line.
- Use one runtime tutoring prompt path.
- Keep backend-owned state minimal.
- Python owns weighted grade computation and monotone grade semantics.
- Keep logging strong enough for diagnostics.
- Future minimal saved state should include a tutor-private comment on current assessment, strategy, and decisions, but that comment does not need to be fed back into the runtime tutor prompt by default.
- Do not reintroduce classifier or policy-family architecture unless later evidence clearly justifies it.
